

var streamz = require('streamz');

var m = require('./index');
m.connect('mongodb://localhost:27017/embark2').then(console.log);
var a = m.collection('applications')
  .find()
  .populate('institution','institutions',{name: 1})
  .pipe(streamz(console.log))
//  .then(console.log,e => console.log(e,e.stack));
  //.pipe(etl.map(console.log));
